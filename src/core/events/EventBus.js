export class EventBus {
    #map;

    constructor() {
        this.#map = new Map();
    }

    publish(event, payload) {
        
    }

    subscribe(event, handler) {
        
    }

    unsubscribe(id) {

    }
}